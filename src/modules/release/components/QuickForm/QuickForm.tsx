import './QuickForm.css';
import {useMachine} from '@xstate/react';
import {releaseMachine} from '../../../../state/release/release';

const ReleaseQuickForm = ({...props}) => {
    return (
        <>
        </>
    );
};
export default ReleaseQuickForm;
